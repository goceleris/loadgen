package loadgen

// Version is the loadgen release version, stamped into Result.LoadgenVersion
// so downstream consumers (probatorium, perfmatrix) can record which loadgen
// build produced a given run.
const Version = "1.4.5"
